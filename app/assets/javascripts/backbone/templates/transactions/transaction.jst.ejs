<a data-toggle="collapse" data-parent="#transactions-accordion" href="#transaction<%= transaction.id %>" class="toggle-transaction-info">
  <table style="width: 100%; table-layout: fixed; color: grey">
    <tr>
      <td style="width: 20%">
        <%= date %>
      </td>
      <td style="width: 35%; word-wrap: break-word; padding: 0 8px">
        <%= group.name %>
      </td>
      <td style="width: 20%; text-align: center">
        <% var members = JSON.parse(group.members) %>
        <%= members.length %>
        <object type='image/svg+xml' data='/assets/nav-people.svg' style="vertical-align: middle">
          Your browser does not support SVG
        </object>
      </td>
      <td style="width: 20%">
        <% if(transaction.action == 'pay') { %>
          <span class="pay-text bold">
            -$<%= transaction.amount %>
          </span>
        <% } else { %>
          <span class="charge-text bold">
            +$<%= transaction.amount %>
          </span>
        <% } %>
      </td>
      <td>
        <span style="font-size: 20px;" class="chevron glyphicon glyphicon-chevron-down"/>
      </td>
    </tr>
  </table>
</a>

<div id="transaction<%= transaction.id %>" class="panel-collapse collapse">
  <hr />
  <table style="width: 100%">
    <tr>
      <td style="width: 50%;">
        <div style="padding: 5px; border-right: 1px solid #e5e5e5;">
          <% for(var i = 0; i < members.length; i++) { %>
            <img src="<%= friends[members[i]].profile_picture_url %>" class="img-circle" style="max-width: 50px" alt="<%=friends[members[i]].display_name %>" />
          <% } %>
        </div>
      </td>
      <td>
        <div style="padding: 5px; word-break: break-word">
          <%= transaction.note %>
        </div>
      </td>
    </tr>    
  </table>
</div>